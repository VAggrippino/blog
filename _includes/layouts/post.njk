---
layout: base.njk
---
<div class="post__title">{{ title | safe }}</div>
{% if subtitle %}
  <div class="post__subtitle">{{ subtitle | safe }}</div>
{% endif %}
Date: <div class="post__date">{% formatted_date date %}</div>
{{ content | safe }}
